{
  "name": "@tiny/redirect",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --bun --watch src/development.ts",
    "build": "tsdown",
    "lint": "bunx --bun eslint --fix src",
    "format": "prettier --write src"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15",
    "drizzle-orm": "^0.45.1",
    "elysia": "^1.4.19",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@tiny/drizzle-orm": "workspace:*",
    "@tiny/eslint": "workspace:*",
    "bun-types": "^1.3.5",
    "eslint": "^9.39.2",
    "prettier": "^3.7.4",
    "tsdown": "^0.18.2"
  }
}
